describe('TESTANDO ALGUMA COISA', () => {
  it('should return one: (IT)',() => {
    const number = 1;
    expect(number).toBe(1)
    // expect(number).not.toBe(1)
  });
});
test('should return Luiz (TEST)', () => {
  const nome = 'Luiz';
  expect(nome).toBe('Luiz')
});
// iguais