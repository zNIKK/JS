import validator from 'validator';
// import _ from 'lodash';
// a importação do modulo vem porem a tipagem não, por isso devemos usar:
// npm i @types/lodash
console.log(validator.isEmail('luiz@gmail.com'));
// console.log(_.clone(1, 2, 3, 4, 5));
